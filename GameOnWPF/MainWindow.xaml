<Window x:Class="GameOnWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        Title="Game"
        RenderOptions.BitmapScalingMode="NearestNeighbor"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True"
        WindowState="Maximized"
        
        mc:Ignorable="d"
        d:DesignWidth="1920"
        d:DesignHeight="1080">

    <!-- Resources (buttons design)-->
    <Window.Resources>

        <!-- menu button style -->
        <Style x:Key="GameMenuButton" TargetType="Button">
            <Setter Property="Width" Value="180"/>
            <Setter Property="Height" Value="50"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Background" Value="#2E2E2E"/>
            <Setter Property="BorderBrush" Value="White"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="0,10"/>

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border"
                                CornerRadius="12"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="10"
                                RenderTransformOrigin="0.5,0.5">

                            <Border.RenderTransform>
                                <ScaleTransform ScaleX="1" ScaleY="1"/>
                            </Border.RenderTransform>

                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>

                        <ControlTemplate.Triggers>

                            <!-- hover effect -->
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border"
                                        Property="Background"
                                        Value="#444444"/>
                                <Setter TargetName="Border"
                                        Property="BorderBrush"
                                        Value="#00FFFF"/>
                            </Trigger>

                            <!-- press effect -->
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Border"
                                        Property="Background"
                                        Value="#1E1E1E"/>
                                <Setter TargetName="Border"
                                        Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.96"
                                                        ScaleY="0.96"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>

                            <!-- disable press -->
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>

                        </ControlTemplate.Triggers>

                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Window.Resources>

    <!-- Main content, buttons, name -->
    <Grid>

        <ContentControl x:Name="MainContent"/>

        <Grid x:Name="MainMenuGrid" Panel.ZIndex="1">

            <Grid.Background>
                <ImageBrush ImageSource="pack://application:,,,/Sprites/Background/MainMenu_background.png"
                            Stretch="UniformToFill"/>
            </Grid.Background>

            <Grid.RowDefinitions>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="4*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="4*"/>
            </Grid.RowDefinitions>

            <!-- game title -->
            <TextBlock Grid.Row="1"
                       Text="Game On WPF"
                       FontSize="42"
                       FontWeight="Bold"
                       Foreground="White"
                       HorizontalAlignment="Center"
                       Margin="0,20"/>

            <!-- start button -->
            <Button Grid.Row="2"
                    Content="Start"
                    Style="{StaticResource GameMenuButton}"
                    HorizontalAlignment="Center"
                    Click="OnGameBuildClick"/>

            <!-- exit button -->
            <Button Grid.Row="3"
                    Content="EXIT"
                    Style="{StaticResource GameMenuButton}"
                    HorizontalAlignment="Center"
                    Click="OnExitGameClick"/>

        </Grid>

    </Grid>
</Window>